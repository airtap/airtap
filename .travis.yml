# http://blog.travis-ci.com/2014-12-17-faster-builds-with-container-based-infrastructure/
sudo: false

addons:
  sauce_connect: true
  hosts:
    - airtap.local

language: node_js

node_js:
  - 13
  - 12
  - 10

services:
  - xvfb

script:
  # TODO: restore tests once they pass locally
  # - npm test
  - npx standard

notifications:
  email:
    recipients:
      - airtap.org@gmail.com
